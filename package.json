{
  "name": "jungle-trail",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "First-person jungle trail. Procedural everything, zero external assets.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "serve": "vite --host 0.0.0.0 --port 8099",
    "shoot": "node tools/shoot.mjs"
  },
  "dependencies": {
    "three": "0.170.0"
  },
  "devDependencies": {
    "playwright": "^1.62.0",
    "vite": "^5.4.0"
  }
}
